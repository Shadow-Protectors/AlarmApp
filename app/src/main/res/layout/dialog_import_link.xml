<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="24dp"
    android:background="@drawable/ic_alarm"
    android:backgroundTint="@color/surface_card">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="📥 Import Shared Location"
        android:textSize="20sp"
        android:textStyle="bold"
        android:textColor="@color/text_primary" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="6dp"
        android:text="Paste a Google Maps link, WhatsApp pin, or coordinates (e.g. 9.9196, 78.1100)"
        android:textSize="13sp"
        android:textColor="@color/text_secondary" />

    <!-- Link Input Field with Paste Icon -->
    <com.google.android.material.textfield.TextInputLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:hint="Maps URL or Coordinates"
        app:boxStrokeColor="@color/primary"
        app:hintTextColor="@color/primary"
        app:endIconMode="clear_text"
        style="@style/Widget.MaterialComponents.TextInputLayout.OutlinedBox">

        <com.google.android.material.textfield.TextInputEditText
            android:id="@+id/etLinkInput"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:inputType="textUri|textMultiLine"
            android:maxLines="3"
            android:textColor="@color/text_primary"
            android:textSize="14sp" />

    </com.google.android.material.textfield.TextInputLayout>

    <!-- Paste From Clipboard Button -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/btnPasteClipboard"
        android:layout_width="wrap_content"
        android:layout_height="38dp"
        android:layout_marginTop="8dp"
        style="@style/Widget.Material3.Button.TextButton"
        android:text="📋 Paste from Clipboard"
        android:textSize="12sp"
        android:textColor="@color/primary" />

    <!-- Parsing Progress & Status -->
    <LinearLayout
        android:id="@+id/layoutParsingStatus"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:visibility="gone"
        android:layout_marginTop="10dp">

        <ProgressBar
            android:layout_width="20dp"
            android:layout_height="20dp"
            android:layout_marginEnd="10dp" />

        <TextView
            android:id="@+id/tvParsingStatus"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Resolving link coordinates..."
            android:textSize="13sp"
            android:textColor="@color/primary" />

    </LinearLayout>

    <!-- Resolved Location Preview Card -->
    <com.google.android.material.card.MaterialCardView
        android:id="@+id/cardResolvedPreview"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="12dp"
        android:visibility="gone"
        app:cardCornerRadius="12dp"
        app:cardElevation="0dp"
        app:strokeColor="@color/status_green"
        app:strokeWidth="1dp"
        app:cardBackgroundColor="@color/status_green_bg">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="12dp">

            <TextView
                android:id="@+id/tvResolvedTitle"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Location Name"
                android:textSize="14sp"
                android:textStyle="bold"
                android:textColor="@color/status_green_text" />

            <TextView
                android:id="@+id/tvResolvedCoords"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="2dp"
                android:text="Lat: 9.9196, Lng: 78.1100"
                android:textSize="12sp"
                android:textColor="@color/text_secondary" />

        </LinearLayout>

    </com.google.android.material.card.MaterialCardView>

    <!-- Dialog Actions -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="end"
        android:layout_marginTop="20dp">

        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnCancelImport"
            android:layout_width="wrap_content"
            android:layout_height="48dp"
            style="@style/Widget.Material3.Button.TextButton"
            android:text="Cancel"
            android:textColor="@color/text_secondary"
            android:layout_marginEnd="8dp" />

        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnConfirmImport"
            android:layout_width="wrap_content"
            android:layout_height="48dp"
            android:text="Set Destination"
            android:textStyle="bold"
            app:cornerRadius="10dp"
            android:backgroundTint="@color/primary" />

    </LinearLayout>

</LinearLayout>
